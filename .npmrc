# Use a hoisted (flat) node_modules layout. The legacy CRA tree and parts of the
# firebase-admin dependency graph assume hoisted resolution; this avoids strict
# peer-dependency breakage during the migration.
node-linker=hoisted

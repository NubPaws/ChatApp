{
  "name": "@chatapp/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared API + socket.io types for the ChatApp backend and frontend.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(@chatapp/shared has no tests)\" && exit 0",
    "lint": "echo \"(@chatapp/shared has no lint step)\" && exit 0"
  }
}

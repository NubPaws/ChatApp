# Copy to `.env.local` (gitignored) to override. Vite only exposes vars prefixed
# with VITE_. Both default to the local backend when unset.
VITE_API_URL=http://localhost:5000
VITE_SOCKET_URL=http://localhost:5000
